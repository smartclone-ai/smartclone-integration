# NPM Configuration for SmartClone Core

# Use exact versions by default
save-exact=true

# Prevent package-lock changes for minor updates
package-lock=true

# Audit level
audit-level=moderate

# Engine strict mode (enforce Node version requirements)
engine-strict=true

# Progress indicator
progress=true
